<%= form_with(model: submission_test, local: true) do |form| %>
  <% if submission_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(submission_test.errors.count, "error") %> prohibited this submission_test from being saved:</h2>

      <ul>
        <% submission_test.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :submission_id %>
    <%= form.text_field :submission_id %>
  </div>

  <div class="field">
    <%= form.label :test_id %>
    <%= form.text_field :test_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
